{
  "name": "tols-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "check": "npm run check -w apps/web",
    "build": "npm run build -w apps/web",
    "dev": "npm run dev -w apps/web"
  }
}
