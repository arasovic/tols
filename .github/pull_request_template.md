## What this changes

<!-- And why. The why is the part that is hard to recover from the diff later. -->

## How it was verified

<!--
`npm run test:coverage` and `npm run check` run in CI, so no need to say they pass.

Worth saying:
- for a bug fix, the test that fails without it
- for anything touching spacing, grids or responsive layout, that you looked at
  it in a browser and at a narrow width. jsdom computes no layout, so the suite
  cannot see those defects.
- for a CLI change, the command you ran and its output
-->

## Notes

<!-- Anything you are unsure about, or deliberately left out. -->
